# Generated files
vendor/
node_modules/
*.log
composer.phar

# Cache directory
cache/

# Local Configuration
.DS_Store

# Environment
.env
.php-version
DOCKER_ENV
docker_tag

# IDE
.vscode/
.idea/

# Demo site: three PHP files, each of which becomes its own lambda.
# The deployment sits at Vercel's per-deployment function ceiling, and the
# card does not need the demo, so it is excluded rather than the card broken.
api/demo/
